[
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "scRNA-seq analysis of PBMC3k",
    "section": "",
    "text": "Overview of the dataset, the question, and the analysis stages.\n\n1. QC & preprocessing"
  },
  {
    "objectID": "01_preprocessing.html",
    "href": "01_preprocessing.html",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "",
    "text": "Single-cell RNA sequencing (scRNA-seq) measures gene expression in individual cells rather than averaging expression across a bulk tissue sample. Where bulk RNA-seq returns one expression profile per sample, scRNA-seq returns one profile per cell, typically for thousands to tens of thousands of cells in a single experiment. This resolution makes it possible to identify cell types and states that would be invisible in bulk data, characterize rare populations, trace differentiation trajectories, and study cell-to-cell heterogeneity within an otherwise “homogeneous” tissue.\nThat resolution comes at a cost. Each cell contributes only a small amount of RNA, so the resulting count data are noisy, sparse, and subject to technical artifacts that bulk RNA-seq does not have to contend with in the same way (dropout, variable capture efficiency, ambient RNA contamination, doublets). A large fraction of the analytical effort in any scRNA-seq workflow — arguably the most consequential fraction, since errors here propagate into every downstream result — goes into preprocessing and quality control: deciding which barcodes represent real cells, which genes are usably detected, and how to normalize the resulting count matrix so that expression values are comparable across cells.\nThis document walks through that preprocessing stage end to end, using the canonical PBMC 3k dataset from 10x Genomics as a worked example. Each section pairs a short conceptual explanation of why a step is done with the runnable R code that does it, so the document can be read either as a tutorial on the concepts or as an executable pipeline (or both, which is the intent of R Markdown).\n\n\n\nThe structure of 10x Genomics data and how it is loaded into R\nConstruction and anatomy of a Seurat object\nData exploration: sparsity, distributions of library size and gene detection\nQuality-control metrics, in particular mitochondrial and ribosomal content\nStatistical vs. literature-based thresholding strategies for filtering\nCell- and gene-level filtering\nTwo normalization strategies — log-normalization and SCTransform — and how their outputs differ\nSaving processed objects for downstream analysis (clustering, cell-type annotation, differential expression — not covered here)\n\nThis document intentionally stops at the end of preprocessing. Clustering, dimensionality reduction, and cell-type annotation are separate stages built on top of a properly QC’d and normalized object, and are natural candidates for a follow-up document.\n\n\n\nPBMC 3k is a peripheral blood mononuclear cell (PBMC) dataset released by 10x Genomics as a public demonstration dataset for their Chromium single-cell 3’ gene expression assay. It contains gene expression measurements for approximately 2,700 single cells, sequenced on an Illumina platform.\nBecause PBMCs are a mixture of well-characterized immune cell types (T cells, B cells, NK cells, monocytes, dendritic cells) with known marker genes, and because the dataset is small enough to process on a laptop, it has become the de facto “hello world” dataset for benchmarking and teaching single-cell workflows — most single-cell tutorials (including Seurat’s own) use it for exactly this reason.\nKey properties of the raw data:\n\n\n\nProperty\nDescription\n\n\n\n\nAssay\n10x Genomics Chromium, 3’ gene expression\n\n\nOrganism\nHuman\n\n\nTissue\nPeripheral blood mononuclear cells (PBMCs)\n\n\nCells (raw)\n~2,700\n\n\nFormat\n10x “filtered” matrix: barcodes.tsv, genes.tsv/features.tsv, matrix.mtx\n\n\nReference genome\nhg19\n\n\nSource\n10x Genomics datasets page\n\n\n\n\n\n\nA 10x Chromium run produces, after alignment and cell calling with cellranger, a sparse count matrix distributed as three files:\n\nmatrix.mtx — the count matrix itself, stored in MatrixMarket sparse format (only nonzero entries are written to disk, since the vast majority of gene × cell entries are zero)\nbarcodes.tsv — the cell barcode corresponding to each column of the matrix (one barcode = one droplet =, ideally, one cell)\ngenes.tsv / features.tsv — the gene identifiers corresponding to each row of the matrix\n\nStoring the matrix in sparse form is not a minor implementation detail — it is what makes single-cell data tractable at all. A dense matrix for 2,700 cells × ~32,000 genes would need to hold ~86 million entries; the sparse representation only needs to store the nonzero ones, which, as we’ll see below, is a small fraction of that."
  },
  {
    "objectID": "01_preprocessing.html#what-this-document-covers",
    "href": "01_preprocessing.html#what-this-document-covers",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "",
    "text": "The structure of 10x Genomics data and how it is loaded into R\nConstruction and anatomy of a Seurat object\nData exploration: sparsity, distributions of library size and gene detection\nQuality-control metrics, in particular mitochondrial and ribosomal content\nStatistical vs. literature-based thresholding strategies for filtering\nCell- and gene-level filtering\nTwo normalization strategies — log-normalization and SCTransform — and how their outputs differ\nSaving processed objects for downstream analysis (clustering, cell-type annotation, differential expression — not covered here)\n\nThis document intentionally stops at the end of preprocessing. Clustering, dimensionality reduction, and cell-type annotation are separate stages built on top of a properly QC’d and normalized object, and are natural candidates for a follow-up document."
  },
  {
    "objectID": "01_preprocessing.html#about-the-dataset",
    "href": "01_preprocessing.html#about-the-dataset",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "",
    "text": "PBMC 3k is a peripheral blood mononuclear cell (PBMC) dataset released by 10x Genomics as a public demonstration dataset for their Chromium single-cell 3’ gene expression assay. It contains gene expression measurements for approximately 2,700 single cells, sequenced on an Illumina platform.\nBecause PBMCs are a mixture of well-characterized immune cell types (T cells, B cells, NK cells, monocytes, dendritic cells) with known marker genes, and because the dataset is small enough to process on a laptop, it has become the de facto “hello world” dataset for benchmarking and teaching single-cell workflows — most single-cell tutorials (including Seurat’s own) use it for exactly this reason.\nKey properties of the raw data:\n\n\n\nProperty\nDescription\n\n\n\n\nAssay\n10x Genomics Chromium, 3’ gene expression\n\n\nOrganism\nHuman\n\n\nTissue\nPeripheral blood mononuclear cells (PBMCs)\n\n\nCells (raw)\n~2,700\n\n\nFormat\n10x “filtered” matrix: barcodes.tsv, genes.tsv/features.tsv, matrix.mtx\n\n\nReference genome\nhg19\n\n\nSource\n10x Genomics datasets page"
  },
  {
    "objectID": "01_preprocessing.html#the-10x-genomics-output-format",
    "href": "01_preprocessing.html#the-10x-genomics-output-format",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "",
    "text": "A 10x Chromium run produces, after alignment and cell calling with cellranger, a sparse count matrix distributed as three files:\n\nmatrix.mtx — the count matrix itself, stored in MatrixMarket sparse format (only nonzero entries are written to disk, since the vast majority of gene × cell entries are zero)\nbarcodes.tsv — the cell barcode corresponding to each column of the matrix (one barcode = one droplet =, ideally, one cell)\ngenes.tsv / features.tsv — the gene identifiers corresponding to each row of the matrix\n\nStoring the matrix in sparse form is not a minor implementation detail — it is what makes single-cell data tractable at all. A dense matrix for 2,700 cells × ~32,000 genes would need to hold ~86 million entries; the sparse representation only needs to store the nonzero ones, which, as we’ll see below, is a small fraction of that."
  },
  {
    "objectID": "01_preprocessing.html#downloading-the-data",
    "href": "01_preprocessing.html#downloading-the-data",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "3.1 Downloading the data",
    "text": "3.1 Downloading the data\nThe PBMC 3k filtered gene-barcode matrix is publicly hosted by 10x Genomics. It is downloaded once and cached locally rather than re-fetched on every knit.\n\n\nCode\n# Source: 10x Genomics public datasets\nurl &lt;- \"https://cf.10xgenomics.com/samples/cell/pbmc3k/pbmc3k_filtered_gene_bc_matrices.tar.gz\"\ndownload_path &lt;- file.path(base_dir, \"pbmc3k_filtered_gene_bc_matrices.tar.gz\")\n\nif (!file.exists(download_path)) {\n  download.file(url, destfile = download_path, mode = \"wb\")\n}\n\n# Extract into the data/ directory\nuntar(download_path, exdir = file.path(base_dir, \"data\"))"
  },
  {
    "objectID": "01_preprocessing.html#loading-a-10x-matrix-into-r",
    "href": "01_preprocessing.html#loading-a-10x-matrix-into-r",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "3.2 Loading a 10x matrix into R",
    "text": "3.2 Loading a 10x matrix into R\nSeurat::Read10X() reads the three-file 10x format described above and assembles it into a single sparse matrix (an object of class dgCMatrix), with gene symbols as row names and cell barcodes as column names.\n\n\nCode\npbmc.data &lt;- Read10X(data.dir = file.path(base_dir, \"data\", \"filtered_gene_bc_matrices\", \"hg19\"))\n\ncat(\"Dataset dimensions:\", dim(pbmc.data), \"\\n\")\n\n\nDataset dimensions: 32738 2700 \n\n\nCode\ncat(\"Data type:\", class(pbmc.data), \"\\n\")\n\n\nData type: dgCMatrix \n\n\nCode\ncat(\"First few gene names:\", head(rownames(pbmc.data)), \"\\n\")\n\n\nFirst few gene names: MIR1302-10 FAM138A OR4F5 RP11-34P13.7 RP11-34P13.8 AL627309.1 \n\n\nCode\ncat(\"First few barcodes:\", head(colnames(pbmc.data)), \"\\n\")\n\n\nFirst few barcodes: AAACATACAACCAC-1 AAACATTGAGCTAC-1 AAACATTGATCAGC-1 AAACCGTGCTTCCG-1 AAACCGTGTATGCG-1 AAACGCACTGGTAC-1"
  },
  {
    "objectID": "01_preprocessing.html#why-sparsity-matters",
    "href": "01_preprocessing.html#why-sparsity-matters",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "3.3 Why sparsity matters",
    "text": "3.3 Why sparsity matters\nIn a typical droplet-based scRNA-seq experiment, the majority of gene × cell entries in the count matrix are zero. This is partly biological (most genes are not expressed in most cell types at any given time) and partly technical — a phenomenon usually called dropout: a transcript that is genuinely present in the cell but is not captured, reverse-transcribed, or sequenced deeply enough to be detected. Distinguishing “true zero” from “dropout zero” is impossible at the level of a single entry, and this ambiguity is one of the central statistical challenges in scRNA-seq analysis (it motivates, among other things, the imputation and modeling choices in methods like SCTransform, discussed later).\n\n\nCode\nnon_zero_pct &lt;- round(100 * sum(pbmc.data &gt; 0) / (nrow(pbmc.data) * ncol(pbmc.data)), 2)\ncat(\"Percentage of non-zero entries in the raw matrix:\", non_zero_pct, \"%\\n\")\n\n\nPercentage of non-zero entries in the raw matrix: 2.59 %\n\n\nStoring this matrix in sparse format (rather than as a dense matrix) is what keeps memory usage manageable — the object only stores the coordinates and values of nonzero entries."
  },
  {
    "objectID": "01_preprocessing.html#automatically-computed-metadata",
    "href": "01_preprocessing.html#automatically-computed-metadata",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "4.1 Automatically computed metadata",
    "text": "4.1 Automatically computed metadata\nOn creation, Seurat automatically computes two metrics for every cell:\n\nnCount_RNA — total UMI (Unique Molecular Identifier) count for the cell, i.e. total library size. UMIs are short random sequences attached to each transcript molecule before amplification, which let Seurat distinguish independent transcript molecules from PCR duplicates of the same molecule — this is why scRNA-seq counts are described as UMI counts rather than raw reads.\nnFeature_RNA — number of distinct genes detected (count &gt; 0) in the cell.\n\n\n\nCode\nhead(pbmc@meta.data)\n\n\n\n  \n\n\n\nCode\nsummary(pbmc$nCount_RNA)\n\n\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n    546    1756    2196    2365    2762   15818 \n\n\nCode\nsummary(pbmc$nFeature_RNA)\n\n\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n  212.0   690.0   816.0   845.5   952.0  3400.0 \n\n\nThese two numbers are the foundation of the QC step below: cells with implausibly low values of either are usually not intact single cells, and cells with implausibly high values are often doublets (two or more cells captured in a single droplet and sequenced together as one barcode)."
  },
  {
    "objectID": "01_preprocessing.html#visualizing-basic-distributions",
    "href": "01_preprocessing.html#visualizing-basic-distributions",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "5.1 Visualizing basic distributions",
    "text": "5.1 Visualizing basic distributions\n\n\nCode\np1 &lt;- ggplot(pbmc@meta.data, aes(x = nCount_RNA)) +\n  geom_histogram(bins = 50, fill = \"lightblue\", color = \"black\", alpha = 0.7) +\n  labs(title = \"Distribution of UMI Counts per Cell\",\n       x = \"Total UMI Counts\", y = \"Number of Cells\") +\n  theme_minimal()\n\np2 &lt;- ggplot(pbmc@meta.data, aes(x = nFeature_RNA)) +\n  geom_histogram(bins = 50, fill = \"lightcoral\", color = \"black\", alpha = 0.7) +\n  labs(title = \"Distribution of Genes Detected per Cell\",\n       x = \"Number of Genes\", y = \"Number of Cells\") +\n  theme_minimal()\n\np3 &lt;- ggplot(pbmc@meta.data, aes(x = nCount_RNA, y = nFeature_RNA)) +\n  geom_point(alpha = 0.6, color = \"darkgreen\") +\n  labs(title = \"UMI Counts vs Genes Detected\",\n       x = \"Total UMI Counts\", y = \"Number of Genes Detected\") +\n  theme_minimal()\n\np1\n\n\n\n\n\n\n\n\n\nCode\np2\n\n\n\n\n\n\n\n\n\nCode\np3\n\n\n\n\n\n\n\n\n\nCode\nggsave(file.path(base_dir, \"plots\", \"umi_distribution.png\"), p1, width = 8, height = 6)\nggsave(file.path(base_dir, \"plots\", \"gene_distribution.png\"), p2, width = 8, height = 6)\nggsave(file.path(base_dir, \"plots\", \"counts_vs_genes.png\"), p3, width = 8, height = 6)\n\nprint(p1)\n\n\n\n\n\n\n\n\n\nCode\nprint(p2)\n\n\n\n\n\n\n\n\n\nCode\nprint(p3)\n\n\n\n\n\n\n\n\n\nThe positive, roughly linear relationship between UMI counts and genes detected (Plot 3) is expected under normal conditions — cells sequenced more deeply will, on average, have more genes detected simply because there is more opportunity to detect each one. Deviations from this relationship (a cell with very high UMI counts but a disproportionately low number of genes detected, for instance) are one of the signatures used to flag doublets or damaged cells.\n\n\nCode\nsaveRDS(pbmc, file.path(base_dir, \"data\", \"pbmc3k.rds\"))"
  },
  {
    "objectID": "01_preprocessing.html#why-mitochondrial-content-matters",
    "href": "01_preprocessing.html#why-mitochondrial-content-matters",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "6.1 Why mitochondrial content matters",
    "text": "6.1 Why mitochondrial content matters\nCells that are dying, ruptured, or otherwise stressed tend to lose cytoplasmic mRNA through membrane damage while retaining mitochondrial mRNA somewhat longer, because mitochondria are membrane-bound organelles. The practical consequence is that a high proportion of a cell’s transcripts mapping to mitochondrial genes is a strong signal of a low-quality or dying cell rather than a genuine biological state (with a few tissue-specific exceptions, e.g. cardiomyocytes, which are not relevant to PBMCs). Human mitochondrial genes are conventionally identified by the MT- gene symbol prefix.\nRibosomal gene content (RPS/RPL prefixes) is tracked as a secondary, optional QC signal — it is more informative as a marker of cell-type-specific translational activity than of cell viability, but unusually low or unusually high values can still flag technical issues.\n\n\nCode\npbmc &lt;- readRDS(file.path(base_dir, \"data\", \"pbmc3k.rds\"))\ncat(\"Loaded PBMC dataset with\", ncol(pbmc), \"cells and\", nrow(pbmc), \"genes\\n\")\n\n\nLoaded PBMC dataset with 2700 cells and 13714 genes\n\n\n\n\nCode\n# Percentage of counts mapping to mitochondrial genes\npbmc[[\"percent.mt\"]] &lt;- PercentageFeatureSet(pbmc, pattern = \"^MT-\")\n\n# Percentage of counts mapping to ribosomal genes (secondary QC signal)\npbmc[[\"percent.ribo\"]] &lt;- PercentageFeatureSet(pbmc, pattern = \"^RP[SL]\")\n\nhead(pbmc@meta.data)"
  },
  {
    "objectID": "01_preprocessing.html#visualizing-qc-metrics",
    "href": "01_preprocessing.html#visualizing-qc-metrics",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "6.2 Visualizing QC metrics",
    "text": "6.2 Visualizing QC metrics\nViolin plots show the distribution of each metric across all cells; scatter plots reveal relationships between metrics that a single-metric view would miss (e.g., whether high mitochondrial content is concentrated among low-library-size cells).\n\n\nCode\np1 &lt;- VlnPlot(pbmc, features = c(\"nFeature_RNA\", \"nCount_RNA\", \"percent.mt\"),\n              ncol = 3, pt.size = 0.1)\np1\n\n\n\n\n\n\n\n\n\nCode\nplot1 &lt;- FeatureScatter(pbmc, feature1 = \"nCount_RNA\", feature2 = \"percent.mt\") +\n  labs(title = \"UMI Counts vs Mitochondrial Percentage\")\n\nplot2 &lt;- FeatureScatter(pbmc, feature1 = \"nCount_RNA\", feature2 = \"nFeature_RNA\") +\n  labs(title = \"UMI Counts vs Genes Detected\")\n\ncombined_scatter &lt;- plot1 + plot2\ncombined_scatter\n\n\n\n\n\n\n\n\n\nCode\nggsave(file.path(base_dir, \"plots\", \"qc_violin.png\"), p1, width = 12, height = 6)\nggsave(file.path(base_dir, \"plots\", \"qc_scatter.png\"), combined_scatter, width = 12, height = 6)"
  },
  {
    "objectID": "01_preprocessing.html#setting-filtering-thresholds",
    "href": "01_preprocessing.html#setting-filtering-thresholds",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "6.3 Setting filtering thresholds",
    "text": "6.3 Setting filtering thresholds\nThere are two broad philosophies for choosing QC thresholds, and the code below demonstrates both so they can be compared.\n1. Statistical / data-driven thresholding. Compute a robust measure of central tendency and spread for each metric — the median and the median absolute deviation (MAD) rather than the mean and standard deviation, because MAD is far less sensitive to the extreme outliers that scRNA-seq QC metrics routinely contain — and flag cells beyond some multiple of MAD from the median (3 MAD is a common convention, roughly analogous to a z-score cutoff but robust to outliers).\n2. Literature-based / conventional thresholding. Use fixed, commonly cited cutoffs from the literature and from widely used tutorials (e.g. the original Seurat PBMC3k vignette): nFeature_RNA between 200 and 2500, and percent.mt below 20%. These are less tailored to the specific dataset but are simple, well-precedented, and easy to communicate.\nNeither approach is objectively “correct” — the data-driven approach adapts to the specific sample but can be thrown off by a dataset that genuinely contains many low-quality cells (inflating the MAD and loosening the threshold accordingly); the literature-based approach is stable but was calibrated on other datasets and assays. In practice, both are computed and compared, and the final choice is a judgment call cross-checked against the QC plots above.\n\n\nCode\ncat(\"nFeature_RNA summary:\\n\")\n\n\nnFeature_RNA summary:\n\n\nCode\nprint(summary(pbmc$nFeature_RNA))\n\n\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n  212.0   690.0   816.0   845.5   952.0  3400.0 \n\n\nCode\ncat(\"\\nnCount_RNA summary:\\n\")\n\n\n\nnCount_RNA summary:\n\n\nCode\nprint(summary(pbmc$nCount_RNA))\n\n\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n    546    1756    2196    2365    2762   15818 \n\n\nCode\ncat(\"\\npercent.mt summary:\\n\")\n\n\n\npercent.mt summary:\n\n\nCode\nprint(summary(pbmc$percent.mt))\n\n\n   Min. 1st Qu.  Median    Mean 3rd Qu.    Max. \n  0.000   1.537   2.031   2.217   2.643  22.569 \n\n\n\n\nCode\n# --- Statistical thresholds (median +/- 3 MAD) ---\nfeature_median &lt;- median(pbmc$nFeature_RNA)\nfeature_mad    &lt;- mad(pbmc$nFeature_RNA)\nfeature_lower  &lt;- feature_median - 3 * feature_mad\nfeature_upper  &lt;- feature_median + 3 * feature_mad\n\nmt_median &lt;- median(pbmc$percent.mt)\nmt_mad    &lt;- mad(pbmc$percent.mt)\nmt_upper  &lt;- mt_median + 3 * mt_mad\n\ncat(\"Proposed filtering thresholds (median +/- 3 MAD):\\n\")\n\n\nProposed filtering thresholds (median +/- 3 MAD):\n\n\nCode\ncat(\"nFeature_RNA: \", feature_lower, \"-\", feature_upper, \"\\n\")\n\n\nnFeature_RNA:  233.3382 - 1398.662 \n\n\nCode\ncat(\"percent.mt:   &lt;\", mt_upper, \"\\n\")\n\n\npercent.mt:   &lt; 4.436775 \n\n\nCode\n# --- Literature-based / conventional thresholds ---\ncat(\"\\nAlternative thresholds (literature-based, Seurat PBMC3k convention):\\n\")\n\n\n\nAlternative thresholds (literature-based, Seurat PBMC3k convention):\n\n\nCode\ncat(\"nFeature_RNA: 200-2500\\n\")\n\n\nnFeature_RNA: 200-2500\n\n\nCode\ncat(\"nCount_RNA:   1000-30000\\n\")\n\n\nnCount_RNA:   1000-30000\n\n\nCode\ncat(\"percent.mt:   &lt;20%\\n\")\n\n\npercent.mt:   &lt;20%"
  },
  {
    "objectID": "01_preprocessing.html#applying-the-filters",
    "href": "01_preprocessing.html#applying-the-filters",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "6.4 Applying the filters",
    "text": "6.4 Applying the filters\nWe proceed with the literature-based thresholds, since they are the more widely used convention for this exact dataset/assay combination and keep the results comparable to other PBMC3k analyses.\n\n\nCode\noriginal_cell_count &lt;- ncol(pbmc)\n\npbmc_filtered &lt;- subset(\n  pbmc,\n  subset = nFeature_RNA &gt; 200 & nFeature_RNA &lt; 2500 & percent.mt &lt; 20\n)\n\nfiltered_cell_count &lt;- ncol(pbmc_filtered)\n\ncat(\"=== CELL FILTERING RESULTS ===\\n\")\n\n\n=== CELL FILTERING RESULTS ===\n\n\nCode\ncat(\"Original cell count: \", original_cell_count, \"\\n\")\n\n\nOriginal cell count:  2700 \n\n\nCode\ncat(\"Filtered cell count:  \", filtered_cell_count, \"\\n\")\n\n\nFiltered cell count:   2693 \n\n\nCode\ncat(\"Cells removed:        \", original_cell_count - filtered_cell_count, \"\\n\")\n\n\nCells removed:         7 \n\n\nCode\ncat(\"Percentage retained:  \",\n    round(100 * filtered_cell_count / original_cell_count, 2), \"%\\n\")\n\n\nPercentage retained:   99.74 %"
  },
  {
    "objectID": "01_preprocessing.html#gene-level-filtering",
    "href": "01_preprocessing.html#gene-level-filtering",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "6.5 Gene-level filtering",
    "text": "6.5 Gene-level filtering\nThe min.cells = 3 filter at object creation used the unfiltered cell population. After removing low-quality cells, it is good practice to re-check gene detection rates, since a gene that only appeared “detected in enough cells” because of low-quality cells should be re-evaluated on the cleaned population.\n\n\nCode\nmin_cells &lt;- 3\n\ncounts_mat &lt;- pbmc_filtered[[\"RNA\"]]@layers[[\"counts\"]]\ngenes_before &lt;- nrow(counts_mat)\n\n# Recover gene names on the counts layer if they were dropped (Seurat v5 Assay5 quirk)\nif (is.null(rownames(counts_mat))) {\n  rn &lt;- tryCatch(Features(pbmc_filtered, assay = \"RNA\"), error = function(e) NULL)\n  if (is.null(rn)) stop(\"No gene names found: rownames(counts) and Features() are both NULL.\")\n  rownames(counts_mat) &lt;- rn\n}\n\ngene_counts &lt;- Matrix::rowSums(counts_mat &gt; 0)\nkeep_genes  &lt;- names(gene_counts[gene_counts &gt;= min_cells])\n\npbmc_filtered &lt;- subset(pbmc_filtered, features = keep_genes)\ngenes_after &lt;- length(keep_genes)\n\ncat(\"=== GENE FILTERING RESULTS ===\\n\")\n\n\n=== GENE FILTERING RESULTS ===\n\n\nCode\ncat(\"Original gene count: \", genes_before, \"\\n\")\n\n\nOriginal gene count:  13714 \n\n\nCode\ncat(\"Filtered gene count:  \", genes_after, \"\\n\")\n\n\nFiltered gene count:   13673 \n\n\nCode\ncat(\"Genes removed:        \", genes_before - genes_after, \"\\n\")\n\n\nGenes removed:         41"
  },
  {
    "objectID": "01_preprocessing.html#why-normalize-at-all",
    "href": "01_preprocessing.html#why-normalize-at-all",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "7.1 Why normalize at all",
    "text": "7.1 Why normalize at all\nRaw UMI counts are not directly comparable between cells, because two cells expressing a gene at the same true level can produce very different raw counts simply due to differences in sequencing depth (total UMIs captured per cell). Normalization aims to remove this technical, depth-driven variation so that differences in expression values between cells reflect biology rather than how deeply each cell happened to be sequenced.\nTwo conceptually different normalization strategies are demonstrated here.\n\n7.1.1 Method 1 — Log-normalization\nThe traditional approach: scale each cell’s counts by its total UMI count, multiply by a constant scale factor (10,000 by convention, chosen simply to bring values into a convenient range), and log-transform (log1p, i.e. log(1 + x), to handle zeros):\n\\[\nx_{ij}^{\\text{norm}} = \\log\\left(1 + \\frac{x_{ij}}{\\sum_j x_{ij}} \\times 10{,}000\\right)\n\\]\nwhere (x_{ij}) is the raw count of gene (i) in cell (j). This is fast, simple, and the historical default, but it treats every cell’s total count as an equally reliable estimate of sequencing depth and does not explicitly model the count distribution’s variance structure.\n\n\nCode\npbmc_log &lt;- pbmc_filtered\npbmc_log &lt;- NormalizeData(pbmc_log, normalization.method = \"LogNormalize\",\n                          scale.factor = 10000)\ncat(\"Log-normalization completed.\\n\")\n\n\nLog-normalization completed.\n\n\n\n\n7.1.2 Method 2 — SCTransform\nSCTransform takes a model-based approach: it fits a regularized negative binomial regression of each gene’s counts against sequencing depth across all cells, pooling information across genes to regularize the per-gene model parameters (since fitting each gene’s model independently would be noisy for lowly-expressed genes). The Pearson residuals from this model — the deviation of each observed count from what the depth-based model predicts — are used as the normalized expression values.\nThis has two practical advantages over log-normalization: it does not require a separate downstream step to identify highly variable genes (the model naturally highlights genes whose variance exceeds what sequencing depth alone would predict), and it tends to better stabilize variance across the full range of expression levels, including for lowly-expressed genes where log-normalization is known to under- or over-correct. The tradeoff is computational cost and, for some users, reduced interpretability of the resulting values relative to a straightforward log ratio.\n\n\nCode\npbmc_sct &lt;- pbmc_filtered\npbmc_sct &lt;- SCTransform(pbmc_sct, verbose = FALSE)\ncat(\"SCTransform normalization completed.\\n\")\n\n\nSCTransform normalization completed."
  },
  {
    "objectID": "01_preprocessing.html#comparing-the-two-methods",
    "href": "01_preprocessing.html#comparing-the-two-methods",
    "title": "Single-Cell RNA-seq Preprocessing and Quality Control",
    "section": "7.2 Comparing the two methods",
    "text": "7.2 Comparing the two methods\nAs a concrete illustration, we compare raw, log-normalized, and SCTransform-normalized values for CD3D, a canonical marker of T cells and one of the more variably expressed genes in a PBMC population — a good stress test for how each method handles a gene with real biological heterogeneity across cell types.\n\n\nCode\nif (\"CD3D\" %in% rownames(pbmc_filtered[[\"RNA\"]])) {\n\n  raw_counts &lt;- GetAssayData(pbmc_filtered, assay = \"RNA\", layer = \"counts\")[\"CD3D\", ]\n  log_norm   &lt;- GetAssayData(pbmc_log, assay = \"RNA\", layer = \"data\")[\"CD3D\", ]\n  sct_norm   &lt;- pbmc_sct@assays$SCT@data[\"CD3D\", ]\n\n  norm_comparison &lt;- data.frame(\n    Raw         = as.numeric(raw_counts),\n    LogNorm     = as.numeric(log_norm),\n    SCTransform = as.numeric(sct_norm)\n  )\n\n  # Drop cells where the gene is undetected under every method\n  norm_comparison &lt;- norm_comparison[rowSums(norm_comparison) &gt; 0, ]\n\n  norm_long &lt;- melt(norm_comparison)\n\n  p_norm_comparison &lt;- ggplot(norm_long, aes(x = variable, y = value, fill = variable)) +\n    geom_boxplot() +\n    labs(title = \"CD3D Expression: Normalization Method Comparison\",\n         x = \"Normalization Method\", y = \"Expression Value\") +\n    theme_minimal() +\n    theme(legend.position = \"none\")\n\n  p_norm_comparison\n\n  ggsave(file.path(base_dir, \"plots\", \"normalization_comparison.png\"),\n         p_norm_comparison, width = 8, height = 6)\n}\n\n\nRaw counts and the two normalized versions live on different scales by construction, so the comparison above is about shape (spread, skew, separation between expressing and non-expressing cells) rather than absolute magnitude — a well-behaved normalization should compress the extreme right-skew of the raw counts while still preserving the separation between CD3D-high (T cell) and CD3D-low/absent (everything else) cells."
  }
]